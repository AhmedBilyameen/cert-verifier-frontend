# Frontend environment — Vite exposes vars prefixed with VITE_
# The dev server proxies /api → http://localhost:5000 (see vite.config.js)
# Only change VITE_API_BASE if you deploy backend to a different host

VITE_API_BASE=/api
VITE_POLYGONSCAN_AMOY=https://amoy.polygonscan.com/tx/
